import { FormShell } from "@/components/form/FormShell";

export const metadata = {
  title: "Obsession Marketing — Start Your Project",
  description: "Tell us about your business and we'll build a site that wins customers.",
};

export default function HomePage() {
  return <FormShell />;
}
