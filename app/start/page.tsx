import { FormShell } from "@/components/form/FormShell";

export const metadata = {
  title: "Start Your Questionnaire — Obsession Marketing",
  description: "Complete our onboarding questionnaire so we can build the perfect website for your business.",
};

export default function StartPage() {
  return <FormShell />;
}
